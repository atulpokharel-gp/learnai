/**
 * Re-export Prisma enums and types for use across the application.
 * The generated Prisma client is in src/generated/prisma/
 * Run `npx prisma generate` to regenerate these files.
 */
export {
  Role,
} from "@/generated/prisma/enums";
